- name: install packages
  hosts: localhost
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.dnf:
      name: {{items}}
      state:
    loop:
      - mysql
      - nginx
      - postfix